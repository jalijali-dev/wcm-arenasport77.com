---
# .cpanel.yml — dijalankan cPanel "Git Version Control" tombol "Deploy HEAD
# Commit". Sinkronin isi repo ke document root, KECUALI file yang memang
# TIDAK PERNAH boleh ikut git (config kredensial asli + folder upload).
#
# Path docroot akun cPanel arenasport77.com (username cPanel: arenasport77) —
# kalau ternyata beda pas dicek di cPanel → Domains, sesuaikan lagi baris
# export DEPLOYPATH di bawah. Lihat docs/HANDOFF.md dan
# wcm1_version1/docs/DEPLOY_WORKFLOW.md (pola yang sama, situs sebelah)
# untuk detail lengkap alur deploy.
deployment:
  tasks:
    - export DEPLOYPATH=/home/arenasport77/public_html/
    - /usr/bin/rsync -av --exclude='.git' --exclude='.cpanel.yml' --exclude='cms-admin/config/database.php' --exclude='cms-admin/config/app.php' --exclude='uploads' ./ $DEPLOYPATH
